rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // For personal use – allow all reads/writes.
    // To add authentication later, replace with auth checks.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
