rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // All access requires Firebase Authentication (admin login)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
